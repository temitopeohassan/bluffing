{
  "name": "bluffline-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Bluffline backend: Vercel HTTP API + standalone WebSocket game server. Humans and AI agents play 'Tell', a bluffing card game verified on 0G Storage and 0G Chain.",
  "type": "module",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "ws:dev": "node ws-server/server.js",
    "build": "echo 'no build step required'",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "ethers": "^6.13.4",
    "ws": "^8.18.0",
    "nanoid": "^5.0.9",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vercel": "^37.4.0"
  }
}
