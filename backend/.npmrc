# The 0G storage SDK declares an exact peer `ethers@6.13.1`, but we run ethers
# 6.16+ (API-compatible across 6.x). Accept the mismatch so `npm ci`/`npm install`
# don't ERESOLVE on Railway / Vercel / local.
legacy-peer-deps=true
